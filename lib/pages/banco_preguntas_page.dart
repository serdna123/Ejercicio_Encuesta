import 'package:flutter/material.dart';

class BancoPreguntasEncuesta extends StatelessWidget {
  const BancoPreguntasEncuesta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("bacno preguntas")),
    );
  }
}
